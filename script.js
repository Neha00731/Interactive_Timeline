// Function to show or hide the details when a timeline item is clicked
function showDetails(itemId) {
    const content = document.getElementById(itemId);
    content.classList.toggle('visible');
}
